# Altoq PWA - Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local or files with real keys to version control

# VAPID Keys for Web Push Notifications
# Generate these using: npx web-push generate-vapid-keys

# Public VAPID Key (exposed to client)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_public_vapid_key_here

# Private VAPID Key (server-side only - KEEP SECRET!)
VAPID_PRIVATE_KEY=your_private_vapid_key_here
